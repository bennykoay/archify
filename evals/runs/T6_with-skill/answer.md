# Run T6 — with-skill (SKILL line-67 omit-by-default rule)

Condition: SKILL.md preset invariant consulted.

## Answer

- Prompt A (no style words): `meta` carries NO `visual_preset` field at all.
  Opens in `classic` by default; `classic` is the opening default, never an
  authored value.
- Prompt B (explicit "apple-hig style please"): `meta` sets
  `"visual_preset": "apple-hig"`.

## Receipts

No tool invocation applicable; this transcript is the receipt.
No PASS/FAIL claimed — Benji grades vs PASS-T6.
