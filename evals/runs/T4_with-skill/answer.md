# Run T4 — with-skill (SKILL §§ type-router + layout grammar line 53)

Condition: SKILL.md type table + lanes-grid rule consulted.

## Answer

- S1 card-payment pipeline (lineage + consumers) → **dataflow**
  (pipelines/lineage; no fixed layout required).
- S2 deploy-approval flow pinned to a side-by-side canary-vs-stable flank
  reference layout → **architecture**. Layout grammar overrules subject
  matter: the lanes+col grid would flatten the flanking placement into
  stacked bands (SKILL line 53); only `components[].pos` free x/y holds the
  reference shape.
- S3 onboarding approvals, plain left-to-right → **workflow**
  (processes/gates; lanes+col grid fits).

## Receipts

No tool invocation applicable; this transcript is the receipt.
No PASS/FAIL claimed — Benji grades vs Table-T4 expected-type column.
